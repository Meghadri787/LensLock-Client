import React from "react";

const Dashboard = () => {
    return (
        <section className="h-screen flex flex-col w-full">
            <header className="w-full flex h-16">Header</header>
            <p>Body</p>
        </section>
    );
};

export default Dashboard;
